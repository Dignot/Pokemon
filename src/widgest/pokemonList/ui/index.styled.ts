import styled from "styled-components";

export const cardsWrappen = styled.div`
    display: grid;
    grid-template-columns: repeat(3, 300px);
    justify-content: center;
    gap: 20px;
`;
export const card = styled.div`
    border: 1px solid black;
`;
